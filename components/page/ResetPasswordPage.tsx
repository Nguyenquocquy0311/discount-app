import ResetPasswordForm from "../composite/form/ResetPasswordForm";

const ResetPasswordPage = () => {
    return (
        <div className="bg-gray-100 flex justify-center items-center h-screen">
            <ResetPasswordForm />
        </div>
    );
};

export default ResetPasswordPage;
